<div class="container">
    <div class="col-md-6 col-md-offset-3">
  <h1 class="text-center">We Received This Gift:<br/></h1>
  <h3><b><%= @item.name%></b></h3>
<!--       <%= "Did you receive the gift from" if @unseen_fulfillments.any? %>
    <ul>
      <% @unseen_fulfillments.each do |f| %>
      <% unseen_fulfillment = "#{f.buyer_name} (#{f.quantity})" %>
      <li><%= link_to unseen_fulfillment, "#" %></li>
    <% end %>
    </ul>
    <%= "Or, someone else?" if @unseen_fulfillments.any? %> -->
    <%= form_for @fulfillment, url: fulfillments_user_create_path do |f|%>
  <div class="form-group">
      <%= f.label :buyer_name, 'Who got this gift for you?' %>
      <%= f.text_field :buyer_name, class:'form-control' %>
  </div>
  <div class="form-group">
      <%= f.label :quantity, "How many did you receive from them?" %>
      <%= f.number_field :quantity, class:'form-control' %>
  </div>
      <%= f.hidden_field :item_id, :value => @item.id %>    
      <%= f.submit "Mark this Item as Purchased", class: 'btn btn-primary btn-submit text-uppercase'%>
    <% end %>
</div>  
</div>  
<!-- set status to 1 -->